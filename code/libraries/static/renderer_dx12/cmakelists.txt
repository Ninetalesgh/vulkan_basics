cmake_minimum_required ( VERSION 3.15.0 )

add_brewing_station_static_library( renderer_dx12 renderer )

target_link_libraries( renderer_dx12 "DirectXTex" )

include_directories( "${BREWING_STATION_EXTERNAL_DIR}/include" )